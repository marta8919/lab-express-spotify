

<div class="list">

    <div class="header">
        <h3>Title</h3>
        <h3>Listen</h3>
    </div>

    {{#each tracks}}
    <div class="row">
        <p>{{this.name}}</p>
        <audio controls controlsList="download" src="{{this.preview_url}}"></audio>
    </div>

    {{/each}}
</div>